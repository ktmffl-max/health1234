# 엑셀이 파일을 열어둔 동안 만드는 임시본
~$*.xlsx

# OneDrive / 윈도우 부산물
desktop.ini
Thumbs.db

# 파이썬
__pycache__/
*.pyc

# 워크북에서 생성되는 로컬 미리보기.
# 배포에 쓰이는 건 docs/index.html 쪽이고, 이 파일은 버전이 올라갈 때마다
# 이름이 바뀌어 리포에 잔해가 쌓이므로 추적하지 않는다.
*_모바일.html
